import 'package:flutter/material.dart';

import '../../textcolumn.dart';

class WorkText extends StatelessWidget {
  const WorkText({super.key});

  @override
  Widget build(BuildContext context) {
    return const TextColumn(
      title: 'No Any Hidden Charges',
      text: 'Make Greeting Cards Free Of Cost No Any Other Hidden Charges',
    );
  }
}
